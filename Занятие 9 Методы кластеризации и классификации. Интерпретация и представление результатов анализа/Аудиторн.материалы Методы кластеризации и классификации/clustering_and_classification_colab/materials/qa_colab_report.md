# QA: версия для Google Colab

**Статус:** ГОТОВО.

## Что проверено

| Проверка | Статус | Результат |
|---|---|---|
| Student notebook | OK | Выполнен сверху вниз без ошибок в локальном Jupyter |
| Teacher notebook | OK | Выполнен сверху вниз без ошибок в локальном Jupyter |
| Ветка Colab с уже загруженным CSV | OK | Смоделирована среда Colab, путь `/content` обрабатывается корректно |
| Ветка Colab с `files.upload()` | OK | Смоделирована загрузка `support_tickets.csv`, файл сохраняется в стандартную папку |
| Относительные локальные пути | OK | Абсолютные пользовательские пути отсутствуют |
| Colab outputs | OK | В Colab результаты направляются в `/content/clustering_and_classification_outputs` |
| Комментарии к коду | OK | Учебные комментарии к программному коду выполнены на русском языке |
| Перезапуск среды | OK | После сброса Colab достаточно повторно запустить notebook и загрузить CSV |
| Автоматическое скачивание | OK | Отключено по умолчанию; включается явно через `DOWNLOAD_RESULTS = True` |

## Контрольные результаты полного запуска

- строк в исходном наборе: 2500;
- итоговое число кластеров: 3;
- создаются `support_tickets_with_clusters.csv`, `cluster_profile.csv`, `classification_metrics.csv`, `classification_predictions.csv`;
- создаются семь учебных графиков;
- обе модели классификации и K-Means выполняются без ошибок.

## Примечание

Файлы из `reference_outputs/` являются результатом контрольного локального запуска и предназначены прежде всего преподавателю для сверки. В Google Colab notebook создаёт результаты заново в `/content/clustering_and_classification_outputs`.
